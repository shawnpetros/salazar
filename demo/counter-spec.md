# Counter Library

A minimal TypeScript counter module with tests.

## Features

- Create a counter with an optional initial value (default 0)
- Increment by 1 or a custom amount
- Decrement by 1 or a custom amount
- Reset to initial value
- Get current count
- Prevent going below zero (floor at 0)
